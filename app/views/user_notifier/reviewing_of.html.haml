= @user.user_name
,

\#{t 'user_notifier.reviewing_of.description', :revision => @revision, :flag => SystemSetting.reviewed_flag}

\#{@url}
- if !@message.blank?
  = t 'user_notifier.reviewing_of.reason'
  = @message
= render 'email_footer'
