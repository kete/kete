= render(:partial => "actions_menu", |
  :locals => { :item => @basket })   |
= render :partial => "topics/content_wrapper_start"
= link_to t('baskets.show.edit'), :action => 'edit', :id => @basket
|
\#{link_to t('baskets.show.back'), :action => 'list'}
/ /main-content-container
/ /main-content-wrapper
#secondary-content-wrapper
/ /secondary-content-warpper
/ /content-wrapper
