<% @title = 'Listing content types' -%>

<h2><%= h(@title) -%></h2>

<table>
  <tr>
    <th>Controller</th>
    <th>Type</th>
    <th>Humanized Type</th>
    <th>Humanized Type Plural</th>
    <th>Description</th>
    <th>Options</th>
  </tr>

<% for content_type in @content_types %>
  <tr>
    <td><%=h content_type.controller %></span></td>
    <td><%=h content_type.class_name %></span></td>
    <td><%=h content_type.humanized %></span></td>
    <td><%=h content_type.humanized_plural %></span></td>
    <td><%=h content_type.description %></td>
    <td><%= link_to 'Edit', :action => 'edit', :id => content_type %></td>
  </tr>
<% end %>
</table>

<% # <ul><li><%= link_to 'Add content type', :action => 'new' %></li></ul> -%>

<%= link_to 'Previous page', { :page => @content_type_pages.current.previous } if @content_type_pages.current.previous %>
<%= link_to 'Next page', { :page => @content_type_pages.current.next } if @content_type_pages.current.next %>

<br />




