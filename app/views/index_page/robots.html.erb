# See http://www.robotstxt.org/wc/norobots.html for documentation on how to use the robots.txt file
User-agent: *
<% @baskets.each do |basket| -%>

#
# <%= basket.name %>
#
<% @controller_names.each do |controller| -%>
# <%= controller %>
Disallow: /<%= basket.urlified_name -%>/<%= controller -%>/new
Disallow: /<%= basket.urlified_name -%>/<%= controller -%>/edit
Disallow: /<%= basket.urlified_name -%>/<%= controller -%>/flag_version
Disallow: /<%= basket.urlified_name -%>/<%= controller -%>/flag_form
Disallow: /<%= basket.urlified_name -%>/<%= controller -%>/history
Disallow: /<%= basket.urlified_name -%>/<%= controller -%>/preview
Disallow: /<%= basket.urlified_name -%>/all/<%= controller -%>/rss
Disallow: /<%= basket.urlified_name -%>/all/<%= controller -%>/rss.xml
Disallow: /<%= basket.urlified_name -%>/all/<%= controller -%>/contributed_by
Disallow: /<%= basket.urlified_name -%>/all/<%= controller -%>/tagged
Disallow: /<%= basket.urlified_name -%>/all/<%= controller-%>/related_to
Disallow: /<%= basket.urlified_name -%>/<%= controller-%>/auto_complete_for_still_image_tag_list
Disallow: /<%= basket.urlified_name -%>/<%= controller-%>/auto_complete_for_web_link_tag_list
Disallow: /<%= basket.urlified_name -%>/<%= controller-%>/auto_complete_for_topic_tag_list
Disallow: /<%= basket.urlified_name -%>/<%= controller-%>/auto_complete_for_document_tag_list
Disallow: /<%= basket.urlified_name -%>/<%= controller -%>/auto_complete_for_comment_tag_list
Disallow: /<%= basket.urlified_name -%>/<%= controller-%>/auto_complete_for_video_tag_list
Disallow: /<%= basket.urlified_name -%>/<%= controller-%>/auto_complete_for_audio_recording_tag_list
Disallow: /<%= basket.urlified_name -%>/<%= controller-%>/selected_image
<% end -%>
# Other
Disallow: /<%= basket.urlified_name -%>/index_page/help_file
Disallow: /<%= basket.urlified_name -%>/account
Disallow: /<%= basket.urlified_name -%>/baskets
Disallow: /<%= basket.urlified_name -%>/members
Disallow: /<%= basket.urlified_name -%>/extended_fields
Disallow: /<%= basket.urlified_name -%>/topic_types
Disallow: /<%= basket.urlified_name -%>/content_types
Disallow: /<%= basket.urlified_name -%>/zoom_dbs
Disallow: /<%= basket.urlified_name -%>/importers
Disallow: /<%= basket.urlified_name -%>/search
<% end -%>

#
# Global
#
Disallow: /*number_of_results_per_page*
